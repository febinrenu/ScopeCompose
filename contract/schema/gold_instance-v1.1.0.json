{
  "$defs": {
    "Annotation": {
      "additionalProperties": false,
      "description": "Provenance of the labels, for the three-stage protocol and kappa reporting.",
      "properties": {
        "annotator_a": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Annotator A"
        },
        "annotator_b": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Annotator B"
        },
        "disagreed": {
          "default": false,
          "description": "True if the two independent annotators differed before discussion.",
          "title": "Disagreed",
          "type": "boolean"
        },
        "notes": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Notes"
        },
        "resolved_by_discussion": {
          "default": false,
          "title": "Resolved By Discussion",
          "type": "boolean"
        },
        "third_reviewer": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Set when a procedure-ambiguous case was routed to the expert reviewer.",
          "title": "Third Reviewer"
        }
      },
      "title": "Annotation",
      "type": "object"
    },
    "Applicability": {
      "additionalProperties": false,
      "description": "The set of cases a branch applies to.\n\n``descriptor`` is the human-readable scope (\"premium-tier cardholders\").\n``attributes`` is the machine-checkable decomposition, when one exists.\nAn empty ``attributes`` list means the default branch (applies to all of U)\nor an unparsed scope; either way the analyser falls back to entailment.",
      "properties": {
        "attributes": {
          "items": {
            "$ref": "#/$defs/ScopeAttribute"
          },
          "title": "Attributes",
          "type": "array"
        },
        "descriptor": {
          "minLength": 1,
          "title": "Descriptor",
          "type": "string"
        },
        "is_default": {
          "default": false,
          "description": "True for the unconditioned branch, whose applicability is the whole case space U.",
          "title": "Is Default",
          "type": "boolean"
        }
      },
      "required": [
        "descriptor"
      ],
      "title": "Applicability",
      "type": "object"
    },
    "AttributeKind": {
      "description": "The shape of an applicability attribute.\n\nTyped so the scope analyser can decide subset/disjoint arithmetically\nrather than asking a language model to do set theory in prose.",
      "enum": [
        "categorical",
        "ordered_tier",
        "numeric_range",
        "boolean",
        "free_text"
      ],
      "title": "AttributeKind",
      "type": "string"
    },
    "Branch": {
      "additionalProperties": false,
      "description": "One (condition, outcome, applicability) triple.\n\nA gold instance is a set of these: exactly one default branch plus zero or\nmore exception branches. This is the representation the composition\noperator assembles and the metric suite scores against, branch by branch.",
      "properties": {
        "applicability": {
          "$ref": "#/$defs/Applicability"
        },
        "branch_id": {
          "minLength": 1,
          "title": "Branch Id",
          "type": "string"
        },
        "condition": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "None for the default branch; the governing condition otherwise.",
          "title": "Condition"
        },
        "explicitness": {
          "$ref": "#/$defs/Explicitness",
          "default": "explicit"
        },
        "outcome": {
          "minLength": 1,
          "title": "Outcome",
          "type": "string"
        },
        "supporting_passage": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Passage id this branch is grounded in. Required for exceptions.",
          "title": "Supporting Passage"
        }
      },
      "required": [
        "branch_id",
        "outcome",
        "applicability"
      ],
      "title": "Branch",
      "type": "object"
    },
    "ConflictType": {
      "description": "The five-class conflict taxonomy.\n\nExtends the prior three-way factual/temporal/opinion split with\n``CONDITIONAL`` -- the class this project exists to handle.",
      "enum": [
        "no_conflict",
        "factual",
        "temporal",
        "opinion",
        "conditional"
      ],
      "title": "ConflictType",
      "type": "string"
    },
    "Construction": {
      "description": "Benchmark tier. Never blend these two when reporting headline numbers.\n\nNATURAL\n    Tier 1. The general rule and its exception were mined from genuinely\n    separate, separately-retrievable documents. This is the primary\n    evidence for the \"naturally occurring, multi-source\" claim that\n    distinguishes this benchmark from ConditionalQA.\nSPLIT\n    Tier 2. A single-document rule/exception pair deliberately split across\n    two synthetic \"documents\". Legitimate and useful -- it isolates the\n    resolution-operator question from the mining question -- but it is not\n    evidence of natural multi-source occurrence.",
      "enum": [
        "natural",
        "split"
      ],
      "title": "Construction",
      "type": "string"
    },
    "Domain": {
      "description": "Benchmark domain. Kept deliberately narrow: financial terms and\nimmigration eligibility keep stakes moderate and ground truth objective.\nMedical and legal are excluded to avoid overclaiming.",
      "enum": [
        "financial_terms",
        "immigration_eligibility",
        "synthetic"
      ],
      "title": "Domain",
      "type": "string"
    },
    "Explicitness": {
      "description": "Whether a condition is stated or must be inferred.\n\nReported separately, never averaged: implicit recovery is the hard case and\nthe one Contrastive Scope Probing exists to address. Averaging the two\nhides exactly the number the method is judged on.",
      "enum": [
        "explicit",
        "implicit"
      ],
      "title": "Explicitness",
      "type": "string"
    },
    "MultiExceptionFlags": {
      "additionalProperties": false,
      "description": "Second-order structure flags.\n\nPopulated by Member B, not A: deciding these requires comparing extracted\nexception branches to EACH OTHER, not just to the default, which only B has.\n\nThe project scope is first-order conditional conflicts. When either flag is\nset the instance is routed to the selection fallback with the specific flag\nrecorded, rather than silently mis-composed. Reporting the two rates\nseparately is what lets the paper say how often real data contains\nexception-to-exception structure versus co-occurring conflicting exceptions.",
      "properties": {
        "crossed": {
          "default": false,
          "description": "Two independent exceptions whose conditions co-occur and whose outcomes disagree.",
          "title": "Crossed",
          "type": "boolean"
        },
        "nested": {
          "default": false,
          "description": "An exception to an exception -- one exception's applicability nests inside another's.",
          "title": "Nested",
          "type": "boolean"
        }
      },
      "title": "MultiExceptionFlags",
      "type": "object"
    },
    "Passage": {
      "additionalProperties": false,
      "description": "A single retrieved passage with the metadata downstream stages need.",
      "properties": {
        "date": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Publication date as YYYY, YYYY-MM, or YYYY-MM-DD. Drives temporal handling.",
          "title": "Date"
        },
        "document_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Identifier of the source document. Two passages sharing a document_id did NOT come from separate documents -- which is what makes a construction='natural' claim checkable rather than asserted.",
          "title": "Document Id"
        },
        "id": {
          "description": "Unique within a record, e.g. 'p0'.",
          "minLength": 1,
          "title": "Id",
          "type": "string"
        },
        "source_type": {
          "$ref": "#/$defs/SourceType",
          "default": "unknown"
        },
        "source_url": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Source Url"
        },
        "text": {
          "minLength": 1,
          "title": "Text",
          "type": "string"
        }
      },
      "required": [
        "id",
        "text"
      ],
      "title": "Passage",
      "type": "object"
    },
    "ScopeAttribute": {
      "additionalProperties": false,
      "description": "One machine-checkable dimension of an applicability set.",
      "properties": {
        "boolean_value": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "BOOLEAN value",
          "title": "Boolean Value"
        },
        "kind": {
          "$ref": "#/$defs/AttributeKind"
        },
        "max_value": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "NUMERIC_RANGE upper bound, inclusive",
          "title": "Max Value"
        },
        "min_value": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "NUMERIC_RANGE lower bound, inclusive",
          "title": "Min Value"
        },
        "name": {
          "description": "e.g. 'card_tier', 'age', 'transaction_origin'",
          "minLength": 1,
          "title": "Name",
          "type": "string"
        },
        "text": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "FREE_TEXT fallback",
          "title": "Text"
        },
        "values": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "CATEGORICAL / ORDERED_TIER members",
          "title": "Values"
        }
      },
      "required": [
        "name",
        "kind"
      ],
      "title": "ScopeAttribute",
      "type": "object"
    },
    "ScopeRelation": {
      "description": "The four-way scope relation between two claims.\n\nDefined jointly over applicability *and* outcome. Deciding only\napplicability -- \"do these scopes overlap?\" -- cannot separate a redundant\nrestatement from a real exception, which is the bug this four-way version\nreplaced a two-way version to fix.\n\nREFINEMENT\n    One applicability set is a proper subset of the other, and the branches\n    are outcome-INCOMPATIBLE on the subset. A genuine exception. Compose.\nDISJOINT\n    Applicability sets do not intersect. Outcome comparison is vacuous.\n    Compose trivially.\nREDUNDANT\n    Applicability sets overlap or nest, but the branches AGREE on outcome\n    throughout the overlap. Not a conflict at all. Merge into one branch.\nOPPOSED\n    Applicability sets overlap, neither contains the other, and outcomes\n    disagree on the overlap. A real contradiction. Fall back to selection.",
      "enum": [
        "refinement",
        "disjoint",
        "redundant",
        "opposed"
      ],
      "title": "ScopeRelation",
      "type": "string"
    },
    "SourceType": {
      "description": "Provenance of a passage. Needed downstream for temporal and credibility\nhandling, and for the credibility-selection baseline.",
      "enum": [
        "official_policy",
        "product_terms",
        "amendment",
        "faq",
        "category_page",
        "government_guidance",
        "third_party",
        "synthetic",
        "unknown"
      ],
      "title": "SourceType",
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "A fully annotated benchmark instance.",
  "properties": {
    "annotation": {
      "$ref": "#/$defs/Annotation"
    },
    "construction": {
      "$ref": "#/$defs/Construction"
    },
    "contract_version": {
      "default": "1.1.0",
      "title": "Contract Version",
      "type": "string"
    },
    "domain": {
      "$ref": "#/$defs/Domain"
    },
    "gold_branches": {
      "items": {
        "$ref": "#/$defs/Branch"
      },
      "title": "Gold Branches",
      "type": "array"
    },
    "gold_conflict_type": {
      "$ref": "#/$defs/ConflictType"
    },
    "gold_multi_exception_flags": {
      "$ref": "#/$defs/MultiExceptionFlags"
    },
    "gold_scope_relation": {
      "anyOf": [
        {
          "$ref": "#/$defs/ScopeRelation"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "gold_scoped_answer": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "The reference answer preserving every valid branch.",
      "title": "Gold Scoped Answer"
    },
    "instance_id": {
      "minLength": 1,
      "title": "Instance Id",
      "type": "string"
    },
    "is_distractor": {
      "default": false,
      "description": "Mandatory negative: a genuine factual/temporal conflict, a disjoint non-conflicting pair, a redundant restatement, or a no-conflict instance. Without these, Spurious-Condition Rate has no denominator.",
      "title": "Is Distractor",
      "type": "boolean"
    },
    "passages": {
      "items": {
        "$ref": "#/$defs/Passage"
      },
      "minItems": 1,
      "title": "Passages",
      "type": "array"
    },
    "query": {
      "minLength": 1,
      "title": "Query",
      "type": "string"
    },
    "selection_answer": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "What a credibility-based selection system would output. The reference against which Suppression Rate is demonstrated -- without it, SR is an assertion rather than a measurement.",
      "title": "Selection Answer"
    },
    "split": {
      "default": "train",
      "description": "train | dev | test -- fixed at release.",
      "title": "Split",
      "type": "string"
    }
  },
  "required": [
    "instance_id",
    "query",
    "domain",
    "construction",
    "passages",
    "gold_conflict_type"
  ],
  "title": "conflict-rag gold_instance v1.1.0",
  "type": "object"
}
